# 讯飞星辰MaaS代理GUI应用 - Python依赖
# 安装命令: pip install -r requirements.txt

# GUI框架 - PySide6用于创建桌面界面（LGPL许可证，开源友好）
PySide6>=6.0.0

# Web框架 - Flask用于代理服务器
Flask==3.0.0
Werkzeug==3.0.1

# OpenAI SDK - 用于调用讯飞星辰MaaS API
# 注意：使用 v1.x 版本，v2.x 有破坏性变更
# httpx 需要兼容版本（openai v1.x 与 httpx 0.28+ 不兼容）
openai>=1.50.0,<2.0.0
httpx>=0.24.0,<0.28.0

# HTTP请求库
requests==2.31.0

# 打包工具 - 用于生成exe可执行文件
PyInstaller==6.3.0

# 日志彩色输出（可选，用于开发调试）
colorlog==6.8.0
